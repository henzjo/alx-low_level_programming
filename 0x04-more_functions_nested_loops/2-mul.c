#include "main.h"
/**
 * mul - multiplication of two number
 * @a: first number that multiply
 * @b:second number that multiply
 *
 * Return: multiplication of the two int
 */
int mul(int a, int b)
{
	return (a * b);
}
